// import natural from "natural";

// const tokenizer = new natural.WordTokenizer();
// const lemmatizer = natural.LancasterStemmer; // Lancaster Stemmer for lemmatization (closest available in `natural`)

/**
 * Normalize and lemmatize the query
 * @param query - User input text
 * @returns Processed text
 */
export const processQuery = async (query: string) => {
  // Add your query processing logic here
  return query;
};
